# 冥想盆（Pensieve）智能体 · 项目总结

> 一句话定位：用户的"外置大脑"——随手丢信息，自动整理长期保存；
> 随时提问，只根据存过的原文回答并标注出处，**没有就说没有，绝不编造**。

---

## 一、目前实现的功能

### 1. 记忆写入与智能打标
- 支持任意**文本消息**直接存入
- 支持**文件解析**：txt / md / pdf / docx / csv / json
- 自动打标（无需用户操作）：LLM 抽取**摘要、标签、人物、事件日期**（"明天""下周三"等相对日期自动换算为绝对日期）
- LLM 不可用时自动降级为**本地规则打标**（正则 + 关键词），功能不中断

### 2. 长期可靠存储
- SQLite 单文件数据库（`pensieve.db`），**原文写入后不可变**
- FTS5 全文索引（自研中文二元分词，解决 FTS5 不支持中文的问题）
- 拷贝单文件即完成备份；支持 JSONL 全量导出

### 3. 精确检索与可信回答
- 混合召回：**SQL 日期硬过滤 + 关键词全文检索**（先 AND 精确、后 OR 召回，bm25 相关度排序）
- 回答三条铁律：①只引用原文逐字回答 ②必须标注出处 #编号供核对 ③无相关记录直接拒答
- 拒答在**检索层**完成，不依赖模型自觉，从机制上杜绝幻觉

### 4. 多用户与隐私隔离（v2.0）
- 注册 / 登录 / 退出，密码 PBKDF2-SHA256 加盐哈希存储（不存明文）
- 每条记忆带 `user_id`，检索、列表、删除严格按用户隔离，**查不到也删不掉别人的记忆**
- 未登录访问自动跳转登录页，API 层 401 拦截
- 旧版单用户数据库**自动迁移**，首个注册用户自动继承历史记忆

### 5. 网页聊天界面
- Flask 后端 + 单页 HTML 前端，气泡式对话
- **意图自动识别**：陈述句 → 存入；疑问句 → 查询；「记住：」前缀强制存入
- 记忆库面板查看最近记录；回答附带出处卡片

### 6. 命令行管理工具
`add / ask / list / show / delete / stats / export` 全套命令，支持 `-u` 指定用户。

---

## 二、搭建思路

### 整体架构

```
【写入路径】
消息/文件 → 文本提取 → LLM 打标(摘要/标签/人物/日期) ─┐
              │（失败自动降级规则打标）                  ▼
              └──────────────► SQLite 长期存储（原文不可变 + FTS5 索引）

【读取路径】
提问 → 解析日期/关键词 → 混合召回(SQL 过滤 + FTS 检索)
     → LLM 组织语言（只能逐字引用检索结果）
     → 输出「回答 + 原文出处」；无结果 → 直接回答"没有"
```

### 核心设计决策

| 决策 | 理由 |
|---|---|
| **准确优先于语义**：SQLite + FTS 确定性检索为主，而非纯向量 RAG | 向量相似度≠相关，无法保证"没有就说没有"；命中即原文才是"绝对准确" |
| **LLM 只做理解，不做事实来源** | 打标和组织语言交给 LLM，但事实永远来自数据库原文，两层用 JSON 契约隔离 |
| **原文不可变 + 强制出处** | 答案可逐字核对，是"可信"的工程基础 |
| **零依赖起步** | 核心纯 Python 标准库，SQLite 内置，LLM 用 urllib 直连，降低部署门槛 |
| **OpenAI 兼容协议** | 智谱/DeepSeek/通义/Kimi 同一套代码，换平台只改 3 行配置 |

### 关键技术点
- **中文二元分词**：中文按两字滑动窗口切分后交给 FTS5，白赚 bm25 排序
- **相对日期换算**：正则 + timedelta，把"下周三"变成可 SQL 精确过滤的 `YYYY-MM-DD`
- **降级设计**：LLM 超时/返回非法 JSON → 自动回退规则路径，系统永远可用
- **Flask 多线程适配**：每个请求独立 SQLite 连接，避免线程亲和性错误

---

## 三、当前面临的问题

| 问题 | 影响 | 严重程度 |
|---|---|---|
| **同义改写召回弱**：关键词检索无法匹配"多少钱≈费用"这类表达 | 可能漏掉相关记忆（漏召回，但绝不会错答） | ⭐⭐⭐ |
| **意图识别靠规则**：陈述/疑问靠特征词判断 | 少数消息会被误判（存成记忆 or 当成查询） | ⭐⭐ |
| **HTTP 明文 + 开发服务器** | 公网部署有安全隐患，并发能力有限 | ⭐⭐（部署前必须解决） |
| **API Key 集中计费** | 所有用户消耗同一个 Key 的余额 | ⭐⭐ |
| **记忆只增不减**：无合并、无遗忘机制 | 长期使用后库膨胀，相似记忆冗余 | ⭐ |
| **无多模态** | 图片/语音信息无法存入 | ⭐ |
| **备份靠手动** | 误删/磁盘故障有数据丢失风险 | ⭐ |

---

## 四、未来改进路线

### 短期（1 周内）
1. **向量语义召回**：sqlite-vec + bge-m3 嵌入作为第三路召回，解决同义改写问题（向量只负责"找到"，回答仍引用原文，准确性不打折）
2. **生产化部署**：waitress 替代开发服务器 + HTTPS（Caddy 自动证书）
3. **网页文件上传**：聊天界面直接拖入 PDF/Word 存入

### 中期（比赛后迭代）
4. **云端部署 + 定时备份**：轻量云服务器 + 每日自动导出
5. **记忆管理增强**：单条编辑、相似记忆合并、批量清理
6. **主动提醒**：event_date 临期时主动推送（"明天是老妈生日"）

### 长期（产品化方向）
7. **多模态记忆**：图片 OCR、语音转文字后走同一写入路径
8. **遗忘与权重**：模仿人类记忆曲线，高频/近期记忆优先召回
9. **更多入口**：微信/飞书/QQ 机器人，消息即记忆
10. **记忆分享**：用户可选择把某条记忆授权给指定的人查看

---

## 五、项目现状一览

| 维度 | 状态 |
|---|---|
| 核心代码 | `pensieve.py` v2.0（约 500 行，纯标准库）+ `web.py` + 两个页面 |
| 测试覆盖 | 写入/打标/检索/拒答/隔离/迁移/降级 全部实测通过 |
| 运行成本 | LLM 打标约几厘钱/条（DeepSeek），存储零成本 |
| 部署形态 | 本机一键启动；公网部署待第③步 |
