# 项目进度

## 多用户账户与隔离 — 已完成

- 已增加注册、登录、注销、不透明 Bearer Session 和用户 UUID。
- 已为对话、消息、长期记忆、embedding 状态和任务记录增加 `user_id`。
- 已在 SQLite 查询和 Chroma metadata/filter 两层强制用户隔离。
- 已增加前端登录/注册/退出入口，保留原有页面风格。
- 已通过 17 项测试，包括用户 A 的记忆不会被用户 B 查询、列出或删除。

## 阶段 1：初始化项目 — 已完成

- 已建立项目目录、技术栈说明、架构文档、实施计划和环境变量模板。
- 已实现 SQLite 记忆数据层，并通过 `init_db.py` 初始化 `memory.db` 和 3 条测试数据。
- 已执行 CRUD 测试：`python -m pytest test_database.py -v`，5 项测试全部通过。

## 向量检索验证

- 已实现 ChromaDB 持久化向量存储，使用内置 `all-MiniLM-L6-v2` 模型生成嵌入。
- 已执行 `python -m pytest test_vector_store.py -v`，2 项测试全部通过。
- 已验证近义语义匹配：查询“这周个人清洁安排”命中“周四洗头”；查询“本周运动计划”命中“周五游泳”。

## LLM 服务验证

- 已实现 OpenAI 兼容的 DashScope LLM 服务，并从 `.env` 读取 API 密钥、服务地址和模型名称。
- 已执行 `python -m pytest test_llm_service.py -v`，4 项 mock 测试全部通过。
- 已验证整合回答提示词包含所有检索记忆、意图识别可区分存储与查询，以及 SDK 配置与 `.env` 配置一致。

## 前端与完整流程验证

- 已完成纯 HTML/CSS/JavaScript 单页界面，包含对话区、原始记录折叠区、记忆卡片、删除操作、深浅色切换和响应式布局。
- 已验证输入“帮我记住周一开会”可成功保存（memory_id=6）。
- 已验证随后输入“这周有什么事”可返回整合回答，并召回周一开会、周四洗头和周五游泳等原始记忆。
- 已验证 8000 端口的健康检查和记忆列表 API 可访问；页面通过 HTTP 提供时会自动使用当前域名，文件方式打开时默认使用 8001 主服务。
- 已通过 JavaScript 语法检查，并确认 820px、480px 移动端响应式规则存在。
